# Development Dockerfile with hot reload for Spring Boot
FROM eclipse-temurin:17-jdk-jammy

WORKDIR /app

COPY mvnw .
COPY .mvn .mvn
COPY pom.xml .
COPY src src

RUN chmod +x mvnw

# Set environment to development profile
ENV SPRING_PROFILES_ACTIVE=dev
ENV SPRING_DEVTOOLS_RESTART_ENABLED=true
ENV SPRING_DEVTOOLS_LIVERELOAD_ENABLED=true
ENV SERVER_PORT=8082

EXPOSE 8082

# Run Spring Boot with Maven in development mode
CMD ["./mvnw", "spring-boot:run", "-Dspring-boot.run.arguments=--spring.devtools.restart.enabled=true"]

